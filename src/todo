Misc TODOs:
- switch from str to Literal for model names
- investigate why every instance in the preprocessed labels (e.g.
  asl2000/train) has the exact same bbox (137, 16, 492, 480) -- confirmed via
  load_data_from_json, zero variance across 14296 instances, and the box
  extends past the 256x256 frame canvas (x up to 492), so it's not even in
  the expected post-crop coordinate space. Raw/original per-instance bboxes
  are real (get_largest_bbox in preprocess.py), so something between raw
  detection and the preprocessed labels JSON is collapsing them to one
  constant value. Found while rewriting
  src/results/dataset_analysis/wlasl_stats_set_level.ipynb (its bbox-based
  plots -- plot_bboxes_on_canvas / plot_dimension_distributions -- are
  correctly implemented but uninformative against this data until the root
  cause is found).

Plotting:
- redo old plots with new visualisations

