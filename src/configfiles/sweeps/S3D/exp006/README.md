# Sweep 6 : 9efnzegw

This sweep follows from [Sweep 5](../exp005/README.md) using the suggested sweep prameters from Aria Wandb's agent. 

Sweep on [Wandb](https://wandb.ai/ljgoodall2001-rhodes-university/Sweeps/sweeps/9efnzegw).

It uses the same hyper parameter range, so the `base.py` file is identical.

Set to run for 50 runs (**new addition**). 
