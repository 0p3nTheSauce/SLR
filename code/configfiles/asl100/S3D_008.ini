[training]
batch_size = 8
update_per_step = 8
max_epoch = 200
early_stopping = {'metric' : ('val','loss'), 'mode': 'min', 'patience': 50, 'min_delta': 0.01}

[optimizer]
eps = 1e-3
backbone_init_lr = 1e-5
backbone_weight_decay = 1e-4
classifier_init_lr = 1e-3
classifier_weight_decay = 1e-7

[model_params]
drop_p = 0.5

[scheduler] 
tmax = 50
eta_min = 1e-5

[data]
num_frames = 32
frame_size = 224


; based on S3D_007

; these were of interest last time
; - increasing number of frames helped
; - deacreasing the equivalent batch size helped
; - decreasing the classifier weight decay helped
; - not decreasing eta min to 0 helped

; and dropped the scheduler tmax to 50